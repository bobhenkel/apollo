apollo {
  api {
    listen = "0.0.0.0"
    port = 8081
    secret = "SuperTestingSecret"
  }

  kubernetes {
    monitoringFrequencySeconds = 1
    healthFrequencySeconds = 1
  }

  scm {
    githubLogin = ""
    githubOauthToken = ""
  }
}