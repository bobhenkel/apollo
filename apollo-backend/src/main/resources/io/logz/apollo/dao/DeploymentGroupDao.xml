<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.logz.apollo.dao.DeploymentGroupDao">
    <select id="getDeploymentGroup" resultType="io.logz.apollo.auth.DeploymentGroup">
        SELECT *
        FROM deployment_groups
        WHERE id = #{id}
    </select>

    <select id="getAllDeploymentGroups" resultType="io.logz.apollo.auth.DeploymentGroup">
        SELECT *
        FROM deployment_groups
    </select>

    <insert id="addDeploymentGroup" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO deployment_groups (name)
        VALUES (#{name})
    </insert>
</mapper>
